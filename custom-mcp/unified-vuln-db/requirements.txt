# Core
mcp>=1.0.0
chromadb>=0.4.0
pydantic>=2.0.0

# Embeddings — always all-MiniLM-L6-v2 (~90MB, 384-dim, fast CPU inference)
sentence-transformers>=2.2.0

# Data fetching
httpx>=0.24.0
aiofiles>=23.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
datasets>=2.14.0  # For HuggingFace

# Rate limiting & retry
tenacity>=8.2.0
ratelimit>=2.2.0

# Utils
anyio>=4.0.0
click>=8.1.0
rich>=13.0.0
python-dotenv>=1.0.0

# Note: torch and transformers are installed automatically by sentence-transformers
