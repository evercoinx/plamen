#!/usr/bin/env bash
# Plamen launcher — finds plamen.py relative to this script (works through symlinks)
PYTHON=$(command -v python3 || command -v python)
exec "$PYTHON" "$(dirname "$0")/plamen.py" "$@"
